import React from 'react';

const Header: React.FC = () => (
  <header className="bg-gray-800 shadow-md text-white p-4 flex items-center justify-between">
    <h1 className="text-xl font-bold">TRVE E-Learning</h1>
  </header>
);

export default Header;
